# Galeria - Photo Albums (dev)

Prereqs: Node 18+, npm

Install and run:

```powershell
npm install
cd web
npm install
npm run dev
```

Notes:
- This scaffold uses `sql.js` (SQLite compiled to WASM) for local metadata storage.
- Thumbnails and import handling are basic stubs for the MVP.
